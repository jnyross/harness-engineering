# Solutions (institutional knowledge)

Categorized knowledge base for Compound Engineering: solved problems, patterns, and learnings. Used by agents for progressive disclosure (query by topic).

Categories: build-errors, performance-issues, security, data-integrity, architecture, testing, workflow, other.

Store entries via `packages/agent` memory APIs; query via `queryLearnings(topic, learningsPath, cwd)`.
